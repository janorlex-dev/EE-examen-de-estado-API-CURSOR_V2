Las leo al inicio de cada turno, antes de responder o tocar código.

En este entorno, las reglas de .cursor/rules (como la que tienes de desarrollo del MVP) se cargan como contexto del proyecto y se aplican automáticamente.
Por eso, cuando me pides cambios, ya trabajo intentando seguir esas instrucciones (simplicidad MVP, escalabilidad, separación UI/lógica/datos, etc.).

Si quieres, también puedo confirmarte explícitamente en cada tarea qué reglas concretas estoy aplicando.

Perfecto.
A partir de ahora, en cada cambio te indicaré de forma breve qué reglas estoy aplicando (por ejemplo: MVP simple, arquitectura modular, sin hardcodeo crítico, escalable y sin dependencias innecesarias).

comprueba si tengo en .cursor\rules esta instrucion y si no la añades.:

instruccion : cada cambio de codigo que se haga ,se hara con la minima modificacion de codigo haciendolo por partes y antes verificar los cambios

Voy a comprobar ahora mismo tus reglas en .cursor/rules para ver si esa instrucción ya existe; si no está, la agrego con el menor cambio posible y te confirmo el resultado.

He revisado el archivo de reglas y esa instrucción no aparece con ese texto, así que voy a añadirla ahora con una edición mínima en la sección de INSTRUCCIÓN DE TRABAJO.

.rules_instrucciones_para_desarrollo_API.mdc
+1
4.	Arquitectura limpia
5.	Cada cambio de código se hará con la mínima modificación posible, aplicándolo por partes y verificando los cambios antes de continuar.
Si falta información:
Listo, comprobado y añadido.

La instrucción no estaba con ese texto, y la agregué en .cursor/rules/.rules_instrucciones_para_desarrollo_API.mdc dentro de INSTRUCCIÓN DE TRABAJO:

Cada cambio de código se hará con la mínima modificación posible, aplicándolo por partes y verificando los cambios antes de continuar.
También verifiqué que quedó guardada correctamente.